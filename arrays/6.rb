# names = ['bob', 'joe', 'susan', 'margaret']
# names['margaret'] = 'jody'

# when using array assignment method, use the index
# of that element rather than the string itself

# correct syntax below

names = ['bob', 'joe', 'susan', 'margaret']
names[3] = 'jody'

p names